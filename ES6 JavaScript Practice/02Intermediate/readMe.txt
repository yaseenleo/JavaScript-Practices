when we use == it just checks equality       
when we use === it checks it is exactly equal to both side     
== operator (Equality)
=== operator (Identity)

in object the object values can be string, number, boolean, data.
in object we use colon rather then equal to and to end the key and value we use coma

trim() = it takes a string representign the calling string stripping whitespaces from both ends

call back functions are simple functions they don't have a name

never use a arrow function inside a object because it will give an error because of arrow function to remove the error we have to bind this by making use cases

actual space and actual token is consider as true
true = 1
false = 0 

Values that interpret as false
false
0
''
null
undefined

anything else rather then these are true